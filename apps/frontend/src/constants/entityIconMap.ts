import type { EntityType } from "../types/EntityType";

export const entityIconMap: Record<EntityType, string> = {
  image: "pi-image",
  "image-series": "pi-images",
  video: "pi-video",
  "3d-model": "pi-table",
};
